export type Product = {
  productName: string;
  productPrice: string;
  manufacturer: string;
  unit: string;
};

export type ProductOption = {
  id: string;
  label: string;
  value: number;
  unit: string;
};